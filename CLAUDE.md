# CLAUDE.md

This file is loaded by Claude Code when working in this repository.

## プロジェクト概要

**Mirror（ミラー）** — 壁打ち用 AI クローンサービス。

特定の人物（クローン主）の判断パターン・経験・知識を学習させた AI クローンを作成し、許可した相手（メンバー）と共有する。本人の時間を奪わずに思考整理・相談を行えるようにする SaaS。

**重要な位置づけ：**

- 「判断する AI」ではなく「壁打ち用 AI」。答えを断定せず、思考整理を手伝う。
- 応答末尾に必ず「最終的には本人に確認してね」のフッターを含める。
- ユーザーのプロファイル・対話履歴は **Mirror のサーバに一切保存しない**。全ユーザーデータはユーザー自身の Google Drive に保管。

## 必読ドキュメント

実装する前に必ず以下を確認すること：

| 優先度  | ファイル                                      | 内容                                                   |
| ------- | --------------------------------------------- | ------------------------------------------------------ |
| 🔴 必読 | `docs/claude_code_instructions.md`            | 実装指示書（Phase 0〜13・進め方・制約）                |
| 🟡 高   | `docs/要件定義書_v4_1.md`                     | 要件定義書（第 4.1 版・差分）                          |
| 🟡 高   | `docs/data_schema_v1.md`                      | JSON / DB スキーマ                                     |
| 🟡 高   | `docs/setup_questionnaire_v2_1_with_memo.md`  | セットアップアンケート v2.1                            |
| 🟢 中   | `docs/prompt_design_v1_clone_response.md`     | クローン応答プロンプト                                 |
| 🟢 中   | `docs/prompt_design_v1_profile_extraction.md` | プロファイル抽出プロンプト                             |
| 📝 記録 | `docs/phase_0_decisions.md`                   | 技術選択の記録（Vercel Postgres / NextAuth.js / pnpm） |

## 実装スタンス

`docs/claude_code_instructions.md` §11 より：

1. このリポジトリ内のドキュメントに答えがあるか確認する。
2. 関連ドキュメントを確認する。
3. それでも不明なら必ず運営者に質問する。
4. **質問せず推測で実装することは絶対に避ける。**

特に以下の領域は必ず確認：

- セキュリティに関わる実装（暗号化・認証・APIキー管理）
- データ保管場所の判断（サーバには認証情報のみ）
- ユーザー体験に大きく影響する UI/UX
- 課金・決済関連

## やってはいけないこと

- ユーザーデータ（プロファイル・対話履歴・APIキー）をサーバ側 DB に保存しない。
- ユーザー同意なしにメトリクスを取得しない。
- 平文の API キーをログに出力しない。

## 進行方針

各 Phase 完了時に動作確認 → コミット → 次の Phase へ。
Phase ごとに必要なパッケージを都度追加する（前倒しで全部入れない）。

@AGENTS.md
